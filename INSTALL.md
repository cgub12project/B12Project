# F.L.A.S.H. — 安裝、執行與測試說明

本文件提供完整的環境建置、App 安裝、首次設定、執行操作與功能測試指南。

---

## 目錄

- [環境需求](#環境需求)
- [方法一：透過 Android Studio 建置（推薦）](#方法一透過-android-studio-建置推薦)
- [方法二：直接安裝 APK](#方法二直接安裝-apk)
- [方法三：命令列建置](#方法三命令列建置)
- [首次啟動必做的設定](#首次啟動必做的設定)
- [執行與操作說明](#執行與操作說明)
- [功能測試指南](#功能測試指南)
- [常見問題排除](#常見問題排除)

---

## 環境需求

### 開發環境

| 項目 | 版本需求 |
|------|----------|
| **Android Studio** | Hedgehog (2023.1.1) 或以上 |
| **JDK** | 17（Android Studio 內建的 JBR 即可） |
| **Android SDK** | API 34（Android 14） |
| **Android Gradle Plugin** | 8.5.0 |
| **Gradle** | 8.7（專案自帶 Gradle Wrapper，不需另外安裝） |
| **Kotlin** | 1.9.24 |
| **NDK** | **不需要**。地端推論的原生程式庫（llama.cpp）已預先編譯為 `.so` 放在 `app/src/main/jniLibs/arm64-v8a/`，一般建置不會重新編譯 C++ |

### 執行環境

| 項目 | 需求 |
|------|------|
| **最低 Android 版本** | Android 8.0（API 26） |
| **目標 Android 版本** | Android 14（API 34） |
| **裝置類型** | 實體 Android 手機 或 Android 模擬器（AVD） |
| **網路** | AI 風險判斷、電話查詢、信箱同步需要網路；通話紀錄清單與已擷取訊息離線可瀏覽 |
| **CPU 架構** | **地端模式需要 arm64-v8a**。非 arm64 裝置仍可正常使用雲端模式，App 會自動判斷並不引導下載模型 |

> **APK 檔案較大（約 62 MB）**，主因是內含 llama.cpp 的 arm64 原生程式庫（5 個 `.so`）。
> 1.9 GB 的地端模型**不包含在 APK 內**，需由 App 內另行下載。

---

## 方法一：透過 Android Studio 建置（推薦）

### 步驟 1：下載專案

```bash
git clone https://github.com/cgub12project/B12Project.git
cd B12Project
```

### 步驟 2：以 Android Studio 開啟

1. 開啟 Android Studio
2. 選擇 **File → Open**
3. 選取專案根目錄（包含 `build.gradle.kts` 的資料夾）
4. 等待 **Gradle Sync** 完成（首次同步約需 3–5 分鐘，視網路速度而定）

### 步驟 3：設定執行裝置

**選項 A — 使用實體手機（建議，尤其要測地端模式時）：**
1. 手機 **設定 → 開發者選項 → USB 偵錯**（若沒有開發者選項，至 **關於手機** 連續點擊 **版本號碼** 7 次）
2. 以 USB 連接電腦
3. 手機出現「允許 USB 偵錯」時選擇「允許」

**選項 B — 使用模擬器（AVD）：**
1. Android Studio → **Device Manager** → **Create Device**
2. 選擇機型（推薦 **Pixel 6** 或以上）→ Next
3. 選擇系統映像 **API 34 (Android 14)** → 下載 → Next → Finish
4. 點擊播放按鈕啟動模擬器

> 模擬器可以完整測試通知擷取、風險判斷、郵件與電話等功能。
> **但地端模式的推論速度不能用模擬器評估**——x86 模擬器是以二進位翻譯執行 arm64 程式碼，速度與真機沒有可比性。

### 步驟 4：建置並執行

1. 確認工具列已選擇目標裝置
2. 點擊綠色 **Run ▶**（或 `Shift + F10`）
3. 建置完成後 App 會自動安裝並啟動

---

## 方法二：直接安裝 APK

專案提供預先建置的 Debug APK：

```
release/app-debug.apk
```

### 安裝步驟

1. 將 `app-debug.apk` 傳輸至 Android 手機（USB、雲端硬碟或通訊軟體）
2. 在手機上開啟該 APK 檔案
3. 若提示「不允許安裝未知來源的 App」，至 **設定 → 安全性 → 允許安裝不明應用程式** 開啟權限
4. 點擊「安裝」→「開啟」

> **注意**：Debug APK 僅供測試用途，使用 debug 簽章。
> 若手機上已安裝過以不同金鑰簽署的版本，安裝會失敗（`INSTALL_FAILED_UPDATE_INCOMPATIBLE`），請先解除安裝舊版。

---

## 方法三：命令列建置

適用於已安裝 JDK 17 與 Android SDK、但不使用 Android Studio 的情況。

```bash
cd B12Project
./gradlew assembleDebug
# 產出：app/build/outputs/apk/debug/app-debug.apk
```

Windows：

```cmd
gradlew.bat assembleDebug
```

若系統沒有設定 `JAVA_HOME`，可指向 Android Studio 內建的 JDK：

```powershell
$env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"
.\gradlew.bat assembleDebug
```

專案也提供容器化建置環境：`docker/Dockerfile.android-build`。

---

## 首次啟動必做的設定

**這一節不能跳過。** App 的資料來源是系統層級的通知與權限，沒有授權的話訊息分頁會是空的。

### 1. 註冊 / 登入帳號（必要）

風險判斷、電話查詢、信箱同步都需要 JWT，**未登入時訊息會停留在「未分析」狀態**。
可直接在 App 註冊頁建立新帳號。

### 2. 開啟通知使用權（必要，否則訊息分頁沒有資料）

App 透過 `NotificationListenerService` 擷取通訊軟體的通知，這個權限**無法由 App 自行要求**，必須手動開啟：

```
系統設定 → 應用程式 → 特殊應用程式存取權 → 通知存取權
→ 找到「F.L.A.S.H. 詐騙感知」→ 開啟
```

不同廠商路徑略有差異（部分機型在「設定 → 通知 → 進階設定 → 通知使用權」）。

開啟後，之後收到的 LINE、簡訊、WhatsApp 等通知才會被擷取。**開啟前已經收到的通知不會補抓。**

### 3. 授權執行時權限（App 內會自動詢問）

| 權限 | 何時詢問 | 不給的後果 |
|------|---------|-----------|
| 簡訊（`READ_SMS`） | 首次進入訊息分頁 | 不會匯入既有簡訊，只剩通知擷取的資料 |
| 通話紀錄（`READ_CALL_LOG`） | 首次進入電話分頁 | 電話清單為空，只能用搜尋框查號碼 |

### 4. 選用設定

| 功能 | 設定方式 |
|------|---------|
| **來電風險浮層** | 系統設定 → 應用程式 → 顯示在其他應用程式上層 → 允許 |
| **來電辨識與封鎖** | 系統設定 → 預設應用程式 → 來電者 ID 與垃圾電話 → 選擇 F.L.A.S.H. |
| **連接 Gmail** | App 內：設定 → 已連接的信箱 → 連接 Gmail（會開啟瀏覽器完成授權後自動回跳） |
| **地端模式** | App 內：設定 → AI 判斷模式 → 地端模式 → 下載模型（約 1.9 GB，建議 Wi-Fi） |

---

## 執行與操作說明

### App 啟動流程

```
啟動 App → Splash 畫面 → （已登入）主畫面 ／ （未登入）登入頁
```

### 認證操作

| 操作 | 說明 |
|------|------|
| **Email 登入** | 輸入已註冊的 Email 與密碼 |
| **註冊帳號** | 填寫顯示名稱、Email、密碼（至少 8 字元），勾選同意條款 |
| **忘記密碼** | 三步驟：輸入 Email → 6 位 OTP → 設定新密碼 |
| **Google 登入** | 透過 Credential Manager 選擇 Google 帳號 |
| **Facebook 登入** | Facebook OAuth |
| **變更密碼** | 設定 → 更改密碼 |

### 主畫面（底部四頁籤）

| 頁籤 | 操作說明 |
|------|----------|
| **訊息** | 已擷取對話的風險列表。上方統計格可點擊切換風險等級篩選；下方 chip 依實際資料動態產生，可依平台篩選。左滑卡片可刪除本機紀錄（會跳確認）。點卡片進入對話詳情 |
| **郵件** | 已同步信件的風險列表，可依風險等級篩選；點信件查看完整內容與 AI 判斷；可封鎖寄件者或提交回報 |
| **電話** | 預設顯示通話紀錄並標示社群回報風險；搜尋框可查任意號碼。點一下號碼展開「封鎖／回報／撥號／更多」；「更多」進入詳情頁 |
| **設定** | 個人資料、文字大小、AI 判斷模式、監控開關，以及我的回報紀錄、可疑帳號列表、封鎖名單、封鎖信箱、已連接信箱等入口 |

### 進階功能操作

| 功能 | 操作路徑 |
|------|----------|
| **查看對話整體判斷** | 訊息頁 → 點卡片 → 對話詳情頁 → 「查看整體對話判斷」（顯示風險等級、詐騙類型、判定理由、防詐建議、詐騙階段） |
| **查看單則訊息分析** | 對話詳情頁 → 點任一則聊天氣泡（會即時重新查詢，需等待數秒） |
| **回報詐騙電話** | 電話頁或電話詳情頁 → 回報 → 選擇詐騙類型 ＋ 描述（至少 10 字）→ 送出 |
| **回報郵件寄件者** | 郵件頁 → 選擇信件的回報動作 → 填寫表單 → 送出 |
| **分享警告** | 電話詳情頁 → 分享 → 自動生成警告文字 → 選擇平台或複製 |
| **查看帳號威脅檔案** | 設定 → 可疑帳號列表 → 點任一帳號 |
| **切換文字大小** | 設定 → 文字大小 → 小／標準／大／特大 |
| **切換 AI 判斷模式** | 設定 → AI 判斷模式 → 雲端／地端 |
| **刪除地端模型** | 設定 → AI 判斷模式 → 刪除地端模型（同時會切回雲端模式） |

---

## 功能測試指南

### 測試前提

- **需先註冊或登入帳號**。本版本已移除早期的 `SampleData.kt` 模擬資料，所有內容都來自真實資料來源（通知擷取、簡訊、通話紀錄、Gmail 同步）與後端 API，未登入時無法取得 AI 判斷結果。
- 後端使用 Cloudflare Tunnel 臨時網址，**可能已隨後端重啟而變更**。若全部訊息都停在「未分析」，多半是後端連不上，見[常見問題排除](#常見問題排除)。

### 1. 認證模組

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 註冊帳號 | 填寫完整表單 → 建立帳號 | 顯示成功，跳轉主畫面 |
| 登入（正確） | 正確 Email + 密碼 | 顯示「登入成功」，跳轉主畫面 |
| 登入（錯誤） | 錯誤密碼 | 顯示後端回傳的錯誤訊息 |
| 空欄位登入 | 不輸入資料直接送出 | 顯示「請輸入電子信箱與密碼」 |
| 忘記密碼三步驟 | Email → OTP → 新密碼 | 各步驟正確切換，可用新密碼登入 |
| 密碼強度指示器 | 註冊時輸入不同強度密碼 | 弱／中／強三段式視覺化 |
| Token 自動換發 | 長時間閒置後再操作 | 不會被登出，背景自動以 refresh_token 換發 |

### 2. 訊息擷取與風險判斷

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 通知擷取 | 開啟通知使用權後，用另一支手機發 LINE／簡訊給測試機 | 訊息分頁出現新卡片（約數秒內自動刷新，不需手動重整） |
| 簡訊匯入 | 首次進入訊息分頁並授權簡訊權限 | 匯入最近 200 則收件匣簡訊 |
| 風險上色 | 檢視訊息列表 | 卡片依風險上色：高危深銹紅／可疑赤陶／安全米白／未分析灰 |
| 統計摘要 | 檢視上方 Intelligence Summary | 高危／可疑／安全數量正確；有未分析項目時顯示提示條 |
| 風險等級篩選 | 點「高危」統計格 | 列表僅剩高危卡片；再點一次還原 |
| 平台篩選 | 點 LINE／簡訊等 chip | 列表僅顯示該平台；chip 依實際資料動態產生 |
| 對話整體判斷 | 進入群組對話 → 查看整體對話判斷 | 顯示風險等級、詐騙類型、判定理由、防詐建議 |
| 詐騙階段 | 進入有風險的對話 | 顯示「詐騙階段：○○」提示條；若後端回傳規則推估結果會標示「（推估）」 |
| 單則即時分析 | 點任一聊天氣泡 | 顯示讀取中，數秒後彈出該則訊息的完整分析 |
| 快取行為 | 離開再重新進入同一對話 | 沒有新訊息時不會重新等待 API（沿用快取） |
| 左滑刪除 | 在訊息卡片上左滑到底 | 跳出確認對話框，確認後才刪除本機紀錄 |

### 3. 郵件模組

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 連接 Gmail | 設定 → 已連接的信箱 → 連接 | 開啟瀏覽器完成 OAuth 後自動回跳 App |
| 信件同步 | 進入郵件分頁 | 顯示同步後的信件與 AI 風險判斷（同步較慢，屬正常） |
| 風險篩選 | 點風險等級 chip | 僅顯示對應等級的信件 |
| 查看信件詳情 | 點任一信件 | 顯示完整內容與判定理由 |
| 封鎖寄件者 | 信件 → 封鎖 | 加入封鎖清單，可在設定 → 封鎖信箱 解除 |

### 4. 電話模組

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 通話紀錄清單 | 授權通話紀錄權限後進入電話分頁 | 顯示近期來電並標示社群回報風險與舉報次數 |
| 號碼查詢 | 搜尋框輸入號碼 | 即時查詢（輸入後約 0.4 秒觸發） |
| 電話詳情 | 點號碼 → 更多 | 顯示舉報次數、詐騙類型與社群回報記錄（回報者身分已遮蔽） |
| 回報號碼 | 詳情頁 → 回報 → 填表 → 送出 | 顯示回報成功 |
| 封鎖號碼 | 點號碼 → 封鎖 | 跳確認後封鎖，可在設定 → 封鎖名單 解除 |
| 離線行為 | 關閉網路後進入電話分頁 | 清單仍顯示（來自通話紀錄與本機快取），並出現離線提示條 |
| 格式警告 | 查看非台灣格式號碼 | 顯示「⚠ 非常規台灣電話格式」 |

### 5. 地端模式

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 模式說明 | 設定 → AI 判斷模式 → 地端模式 | 跳出說明對話框，標示需下載約 1.9 GB 並揭露已知誤判限制 |
| 模型下載 | 對話框 → 下載模型 | 顯示下載進度；下載完成後校驗 SHA-256 |
| 中斷續傳 | 下載中殺掉 App 後重新進入 | 顯示「已下載 XXX MB，這次會從中斷處繼續」 |
| 斷線重連 | 下載中關閉網路再開啟 | 顯示「連線中斷，重新連線並從中斷處續傳…」，不需手動重試 |
| 非 arm64 裝置 | 在 x86 裝置上選地端模式 | 顯示裝置不支援的說明，不會引導下載 |
| 刪除模型 | 設定 → AI 判斷模式 → 刪除地端模型 | 模型與暫存檔清除，自動切回雲端模式 |

### 6. 設定模組

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 文字大小 | 設定 → 文字大小 → 特大 | 全 App 文字與設定頁圖示等比放大 |
| 監控開關 | 切換各項開關 | 狀態同步至後端使用者設定 |
| 我的回報紀錄 | 設定 → 我的回報紀錄 | 顯示個人歷史回報 |
| 登出 | 設定 → 登出 | 清除 Token，回到登入頁 |

---

## 常見問題排除

### 訊息分頁完全沒有資料

```
最常見原因：通知使用權沒有開啟。
1. 系統設定 → 應用程式 → 特殊應用程式存取權 → 通知存取權 → 允許 F.L.A.S.H.
2. 注意：開啟權限「之前」收到的通知不會補抓，請重新發一則測試訊息
3. 若只想看簡訊，確認已授權簡訊權限（首次進入訊息分頁時會詢問）
```

### 所有訊息都顯示「未分析」

```
原因 A：尚未登入 → 風險判斷需要 JWT，請先登入
原因 B：後端連不上 → Cloudflare Tunnel 網址已變更
解決：
1. 確認裝置有網路
2. 向後端開發者索取最新 API 網址
3. 更新 app/src/main/java/com/frauddetector/network/ApiConfig.kt 的 BASE_URL 後重新建置

※「未分析」是刻意設計的狀態：拿不到 AI 判斷時不會顯示為「安全」，
   避免在後端異常時給使用者錯誤的安心感。
```

### Gradle Sync 失敗

```
1. 確認使用 JDK 17（File → Settings → Build Tools → Gradle → Gradle JDK）
2. File → Invalidate Caches → Restart
3. 刪除專案的 .gradle 資料夾後重新同步
```

### APK 安裝失敗

```
INSTALL_FAILED_UPDATE_INCOMPATIBLE
→ 裝置上已有以不同金鑰簽署的同名 App，請先解除安裝舊版再安裝

安裝被封鎖
→ 設定 → 安全性 → 允許安裝不明來源應用程式
```

### 地端模型下載很慢或一直中斷

```
1. 後端目前透過 Cloudflare Quick Tunnel 提供下載，速度約 1.4 MB/s，
   1.9 GB 全程需 20 分鐘以上，屬正常現象
2. 建議在 Wi-Fi 環境下載
3. 下載中斷會自動從斷點續傳，不需要重新開始；
   已下載的暫存檔會保留，離開 App 再進來也還在
```

### 地端模式判斷很慢

```
在模擬器上：屬正常。x86 模擬器以二進位翻譯執行 arm64 程式碼，
速度沒有參考價值，請改用實體 arm64 手機測試地端推論。
```

### 模擬器執行緩慢

```
1. 確認已啟用硬體虛擬化（Intel HAXM / WHPX / AMD Hypervisor）
2. AVD 設定中分配至少 2 GB RAM
3. 使用 x86_64 系統映像
```

---

## 技術支援

如遇到其他問題，請透過 GitHub Issues 回報：

https://github.com/cgub12project/B12Project/issues
