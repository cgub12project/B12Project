# F.L.A.S.H. — 安裝、執行與測試說明

本文件提供完整的環境建置、App 安裝、執行操作與功能測試指南。

---

## 目錄

- [環境需求](#環境需求)
- [方法一：透過 Android Studio 建置（推薦）](#方法一透過-android-studio-建置推薦)
- [方法二：直接安裝 APK](#方法二直接安裝-apk)
- [方法三：命令列建置](#方法三命令列建置)
- [執行與操作說明](#執行與操作說明)
- [功能測試指南](#功能測試指南)
- [常見問題排除](#常見問題排除)

---

## 環境需求

### 開發環境

| 項目 | 版本需求 |
|------|----------|
| **Android Studio** | Hedgehog (2023.1.1) 或以上 |
| **JDK** | 17（Android Studio 內建） |
| **Android SDK** | API 34（Android 14） |
| **Gradle** | 8.5+（專案自帶 Gradle Wrapper） |
| **Kotlin** | 1.9.24 |

### 執行環境

| 項目 | 需求 |
|------|------|
| **最低 Android 版本** | Android 8.0（API 26） |
| **目標 Android 版本** | Android 14（API 34） |
| **裝置類型** | 實體 Android 手機 或 Android 模擬器（AVD） |
| **網路** | 需要網路連線（API 串接功能） |

---

## 方法一：透過 Android Studio 建置（推薦）

### 步驟 1：下載專案

```bash
git clone https://github.com/cgub12project/B12Project.git
cd B12Project
```

### 步驟 2：以 Android Studio 開啟

1. 開啟 Android Studio
2. 選擇 **File → Open**
3. 選取專案根目錄（包含 `build.gradle.kts` 的資料夾）
4. 等待 **Gradle Sync** 完成（首次同步約需 3-5 分鐘，視網路速度而定）

### 步驟 3：設定執行裝置

**選項 A — 使用實體手機：**
1. 在手機上開啟 **設定 → 開發者選項 → USB 偵錯**（如未看到開發者選項，請至 **關於手機** 連續點擊 **版本號碼** 7 次）
2. 用 USB 線連接電腦
3. 手機上出現 **允許 USB 偵錯** 提示時，選擇「允許」

**選項 B — 使用模擬器（AVD）：**
1. 在 Android Studio 中，點擊 **Device Manager**（右上角手機圖示）
2. 點擊 **Create Device**
3. 選擇機型（推薦 **Pixel 6**）→ Next
4. 選擇系統映像 **API 34 (Android 14)** → 下載 → Next → Finish
5. 點擊播放按鈕啟動模擬器

### 步驟 4：建置並執行

1. 在 Android Studio 上方工具列，確認已選擇目標裝置
2. 點擊綠色 **Run ▶** 按鈕（或按 `Shift + F10`）
3. 等待建置完成，App 將自動安裝並啟動

---

## 方法二：直接安裝 APK

專案提供預先建置的 Debug APK，位於：

```
release/app-debug.apk
```

### 安裝步驟

1. 將 `app-debug.apk` 傳輸至 Android 手機（透過 USB、雲端硬碟或通訊軟體）
2. 在手機上開啟 APK 檔案
3. 如提示「不允許安裝未知來源的 App」，請至 **設定 → 安全性 → 允許安裝不明應用程式** 開啟權限
4. 點擊「安裝」
5. 安裝完成後，點擊「開啟」即可執行

> **注意**：Debug APK 僅供測試用途，未經過正式簽章。

---

## 方法三：命令列建置

適用於無 Android Studio 環境但已安裝 JDK 17 與 Android SDK 的情況。

```bash
# 進入專案目錄
cd B12Project

# 使用 Gradle Wrapper 建置 Debug APK
./gradlew assembleDebug

# 建置完成後，APK 位於：
# app/build/outputs/apk/debug/app-debug.apk
```

在 Windows 上使用：
```cmd
gradlew.bat assembleDebug
```

---

## 執行與操作說明

### App 啟動流程

```
啟動 App → Splash 畫面（進度條動畫）→ 登入頁面
```

### 認證操作

| 操作 | 說明 |
|------|------|
| **Email 登入** | 輸入已註冊的 Email 與密碼，點擊「登入系統」 |
| **註冊帳號** | 點擊「立即註冊 →」，填寫顯示名稱、Email、密碼（至少 8 字元），勾選同意條款 |
| **忘記密碼** | 點擊「忘記密碼？」，依序完成 3 步驟：輸入 Email → 輸入 6 位 OTP 驗證碼 → 設定新密碼 |
| **Google 登入** | 點擊 Google 按鈕，選擇 Google 帳號 |
| **Facebook 登入** | 點擊 Facebook 按鈕，選擇 Facebook 帳號 |

### 主畫面（底部四頁籤）

| 頁籤 | 操作說明 |
|------|----------|
| **訊息** | 瀏覽詐騙訊息警報列表；可依風險等級（高危/可疑/安全）及來源平台（LINE/簡訊/WhatsApp/Messenger）篩選；點擊任一警報可查看對話詳情與 AI 分析結果 |
| **郵件** | 瀏覽可疑郵件列表；可依郵件供應商（Gmail/Outlook）篩選 |
| **電話** | 瀏覽電話號碼詐騙資料庫；可使用搜尋框即時搜尋號碼；點擊號碼查看詳情（社群回報記錄、撥號/封鎖/回報/分享操作） |
| **設定** | 查看個人資料、切換各項監控與通知設定、登出 |

### 進階功能操作

| 功能 | 操作路徑 |
|------|----------|
| **查看對話詳情** | 訊息頁 → 點擊任一警報 → 對話詳情頁（階段式聊天氣泡 + AI 風險標記） |
| **查看帳號威脅檔案** | 對話詳情頁 → 點擊帳號名稱 → 帳號威脅頁（風險環形圖 + 觸發規則 + 證據摘要） |
| **回報詐騙電話** | 電話詳情頁 → 點擊「回報」→ 選擇詐騙類型 + 輸入描述（至少 10 字）→ 送出 |
| **回報可疑帳號** | 帳號威脅頁 → 點擊「封鎖回報」→ 選擇詐騙類型 + 平台 + 帳號資訊 → 送出 |
| **分享警告** | 電話詳情頁或帳號威脅頁 → 點擊「分享」→ 自動生成警告文字 → 選擇平台（LINE/WhatsApp/Messenger/SMS）或複製到剪貼簿 |

---

## 功能測試指南

### 測試帳號

App 內建模擬資料（`SampleData.kt`），不需登入即可瀏覽大部分功能畫面。如需測試 API 串接功能，可透過 App 註冊頁面建立新帳號。

> **注意**：後端 API 使用 Cloudflare Tunnel 臨時網址，可能會隨後端重啟而變更。如遇到連線失敗，請聯繫開發者取得最新 API 位址。

### 測試項目清單

#### 1. 認證模組測試

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 註冊帳號 | 填寫完整表單 → 點擊「建立帳號」 | 顯示成功對話框，自動跳轉至主畫面 |
| 登入（正確） | 輸入已註冊的 Email + 密碼 → 登入 | 顯示「登入成功」，跳轉至主畫面 |
| 登入（錯誤） | 輸入錯誤密碼 → 登入 | 顯示「登入失敗」錯誤提示 |
| 空欄位登入 | 不輸入任何資料 → 登入 | 顯示「請輸入電子信箱與密碼」提示 |
| 忘記密碼 Step 1 | 輸入已註冊 Email → 發送驗證碼 | 切換至 OTP 輸入畫面 |
| 忘記密碼 Step 2 | 輸入 6 位 OTP → 確認 | 切換至新密碼設定畫面 |
| 忘記密碼 Step 3 | 輸入新密碼 + 確認密碼 → 重置 | 顯示成功提示，可用新密碼登入 |
| 密碼強度指示器 | 註冊時輸入不同長度密碼 | 弱（紅）/ 中（橘）/ 強（綠）三段式視覺化 |

#### 2. 訊息模組測試

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 檢視警報列表 | 進入「訊息」頁籤 | 顯示高危/可疑/安全統計 + 警報列表 |
| 風險等級篩選 | 點擊「高危」/「可疑」/「安全」標籤 | 列表僅顯示對應等級的警報 |
| 來源篩選 | 點擊平台篩選按鈕（LINE/簡訊等） | 列表僅顯示對應平台的警報 |
| 查看對話詳情 | 點擊任一警報項目 | 跳轉至對話詳情頁，顯示階段式聊天 |
| AI 風險標記 | 在對話詳情頁檢視 | 可疑訊息顯示紅/橘色標記與風險原因 |

#### 3. 電話模組測試

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 電話號碼搜尋 | 在搜尋框輸入號碼 | 即時過濾顯示匹配結果 |
| 查看電話詳情 | 點擊任一電話號碼 | 顯示詳情頁（風險等級、社群回報） |
| 回報詐騙號碼 | 詳情頁 → 回報 → 填寫表單 → 送出 | 顯示「回報成功」（需已登入） |
| 分享警告 | 詳情頁 → 分享 | 跳轉至分享頁面，顯示警告預覽 |

#### 4. 郵件模組測試

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 檢視郵件列表 | 進入「郵件」頁籤 | 顯示可疑郵件列表（寄件人、主旨、風險等級） |
| 供應商篩選 | 點擊 Gmail / Outlook 篩選 | 列表僅顯示對應供應商的郵件 |

#### 5. 設定模組測試

| 測試項目 | 操作步驟 | 預期結果 |
|----------|----------|----------|
| 檢視個人資料 | 進入「設定」頁籤 | 顯示使用者名稱與 Email |
| 切換監控開關 | 切換「訊息即時監控」等開關 | 開關狀態正確切換（UI 回饋） |
| 登出 | 點擊「登出」 | 清除 Token，跳轉至登入頁面 |

---

## 常見問題排除

### Gradle Sync 失敗

```
問題：Gradle sync failed
解決：
1. 確認已安裝 JDK 17
2. File → Invalidate Caches → Restart
3. 刪除 .gradle 資料夾後重新同步
```

### 無法連線至後端 API

```
問題：連線失敗 / 伺服器無回應
原因：後端使用 Cloudflare Tunnel 臨時網址，可能已變更
解決：
1. 確認手機/模擬器有網路連線
2. 聯繫開發者取得最新 API URL
3. 更新 app/src/main/java/com/frauddetector/network/ApiConfig.kt 中的 BASE_URL
```

### 模擬器執行緩慢

```
解決：
1. 確認電腦已啟用硬體虛擬化（Intel HAXM 或 AMD Hypervisor）
2. 在 AVD 設定中，分配至少 2GB RAM
3. 使用 x86_64 系統映像（而非 ARM）
```

### APK 安裝失敗

```
問題：安裝被封鎖 / 簽章不符
解決：
1. 設定 → 安全性 → 允許安裝不明來源應用程式
2. 如為更新安裝，先卸載舊版本再安裝
```

---

## 技術支援

如遇到其他問題，請透過 GitHub Issues 回報：

https://github.com/cgub12project/B12Project/issues
