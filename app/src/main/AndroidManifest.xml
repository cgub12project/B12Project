<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- 響鈴時顯示風險標籤懸浮視窗用（CallRiskOverlay），需要使用者到系統設定手動開啟 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:name=".FlashApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.FraudDetector">

        <!-- Splash -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.FraudDetector.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity android:name=".ui.auth.LoginActivity" />

        <!-- Register -->
        <activity android:name=".ui.auth.RegisterActivity" />

        <!-- Forgot Password (3 steps) -->
        <activity android:name=".ui.auth.ForgotPasswordActivity" />

        <!-- Google Account Picker -->
        <activity android:name=".ui.auth.GooglePickerActivity" />

        <!-- Facebook Account Picker -->
        <activity android:name=".ui.auth.FacebookPickerActivity" />

        <!-- Mail OAuth Callback：後端完成 Gmail/Outlook 授權後，把瀏覽器導回這個深層連結 -->
        <activity
            android:name=".ui.auth.MailOAuthCallbackActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="flashapp" android:host="mail-callback" />
            </intent-filter>
        </activity>

        <!-- Facebook SDK -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <!-- Main (with Bottom Navigation) -->
        <activity android:name=".ui.main.MainActivity" />

        <!-- Thread Detail -->
        <activity android:name=".ui.detail.ThreadDetailActivity" />

        <!-- Account Threat Profile -->
        <activity android:name=".ui.detail.AccountDetailActivity" />

        <!-- Phone Detail -->
        <activity android:name=".ui.detail.PhoneDetailActivity" />

        <!-- Blocked Numbers List -->
        <activity android:name=".ui.detail.BlockedNumbersActivity" />

        <!-- Blocked Emails List -->
        <activity android:name=".ui.detail.BlockedEmailsActivity" />

        <!-- Connected Mail Accounts (Gmail/Outlook) -->
        <activity android:name=".ui.detail.MailAccountsActivity" />

        <!-- Real Mail Message Detail -->
        <activity android:name=".ui.detail.MailMessageDetailActivity" />

        <!-- Full Report -->
        <activity android:name=".ui.detail.ReportFullActivity" />

        <!-- Share Warning -->
        <activity android:name=".ui.detail.ShareWarningActivity" />

        <!-- My Reports -->
        <activity android:name=".ui.detail.MyReportsActivity" />

        <!-- Suspect Accounts List -->
        <activity android:name=".ui.detail.SuspectAccountsActivity" />

        <!-- Notification Listener Service -->
        <service
            android:name=".service.NotificationCaptureService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Call Screening Service：只有使用者把 FLASH 設為預設來電攔截 App 後系統才會綁定 -->
        <service
            android:name=".service.CallBlockingService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

    </application>
</manifest>
